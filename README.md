# DjangoDigitRecognition

Web application written in React that allows user to draw an digit and sent it to classification

Django server processes the image and outputs the result back to the React client
