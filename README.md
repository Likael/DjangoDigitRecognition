# DjangoImageRecognition

Web application written in React that allows user to draw an image and sent it to classification

Django server processes the image and outputs the result back to the React client

